Addition

#include <stdio.h>

int main()
{
    int num1, num2, sum;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    sum = num1 + num2;

    printf("The sum is: %d\n", sum);

    return 0;
}


Subtraction

#include <stdio.h>

int main()
{
    int num1, num2, result;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    result = num1 - num2;

    printf("%d", result);

    return 0;
}

multiplication

#include <stdio.h>

int main() 
{
    int a,b,c;

    printf("enter two integers: ");
    scanf("%d %d", &a,&b);

    c = a * b;

    printf(" %d",c);

    return 0;
}

 division

#include <stdio.h>

int main()
{
    float a, b, result;

    scanf("%f %f", &a, &b);
    result = a / b;

    printf("%.2f", result);

    return 0;
}

